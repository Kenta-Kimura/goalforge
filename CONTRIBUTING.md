# GoalForge Contribution Guide

この文書は、GoalForgeの開発フローと品質基準の正本です。短期的に動く実装よりも、保守性・拡張性・一貫性を優先します。

## 開発フロー

1. **GitHub First**: 実装前にGitHub上の最新状態を取得し、ローカルだけを基準に進めません。
2. **Issueベース開発**: 背景、目的、実装内容、完了条件をIssueに整理します。
3. **作業ブランチ**: `main`を安定版とし、新機能は`feature/<topic>`、バグ修正は`fix/<topic>`で作業します。文書やリファクタリングのみの変更には`docs/<topic>`、`refactor/<topic>`を使用できます。
4. **Pull Request**: 実装と確認が完了したらPull Requestを作成します。
5. **mainへマージ**: 必須確認を通過し、変更内容を確認してから`main`へマージします。

## コミット規約

コミットは小さく、意味のある単位で作成します。メッセージは次の接頭辞を使用します。

- `feat:` 機能追加
- `fix:` バグ修正
- `refactor:` 振る舞いを変えない構造改善
- `docs:` 文書のみの変更
- `test:` テストの追加・修正
- `style:` 動作に影響しない書式や見た目の変更
- `chore:` ビルド、設定、保守作業

機能追加とリファクタリングを同じコミットへ混在させないでください。

## Issue

Issueには最低限、次を記載します。

- 背景
- 目的
- 実装内容
- 完了条件

設計判断が必要な場合は、候補とトレードオフも残します。

## Pull Request

Pull Requestは日本語で作成し、最低限、次を記載します。

- 概要
- 背景
- 変更内容
- 動作確認
- DB変更（なしの場合も明記）
- Breaking Change（なしの場合も明記）
- UI変更がある場合のスクリーンショット

## 大きな設計変更

次の変更は、実装前にIssueまたはPull Requestで提案し、承認後に着手します。

- DB設計変更
- ディレクトリ構成変更
- ライブラリの追加・削除
- UIの全面変更
- アーキテクチャ変更

提案には次を含めます。

- 理由
- メリット
- デメリット
- 影響範囲

## SQLite Migration

- SQLiteスキーマを変更するときは、`src-tauri/migrations/`へ連番のMigrationを追加します。
- 適用済みMigrationは編集せず、新しいMigrationで前進させます。
- 既存データを破壊・欠落させない移行手順にします。
- テーブル再作成が必要な場合は、データコピー、制約、インデックス、外部キーを確認します。
- Migrationの適用バージョンは`schema_migrations`で管理します。
- スキーマ変更時は[データベース設計](docs/database.md)も更新します。

## テストと確認

`main`へ入れる前に、変更範囲に応じて次を確認します。

```bash
cd src-tauri && cargo check
npm run build
npm test
npm run test:validation
```

加えてTauri版を起動し、変更箇所と主要画面操作を確認します。少なくとも教材管理、演習、解答履歴、データ保存に影響がないことを確認してください。実行できない項目は、理由と未確認範囲をPull Requestへ記載します。

## ドキュメント更新

機能追加や仕様変更では、必要に応じて次を同じ変更内で更新します。

- `README.md`
- `docs/architecture.md`
- `docs/database.md`
- `docs/roadmap.md`
- `docs/ui.md`

UI文言、設計説明、完了条件が実装と食い違わない状態を維持します。

## リファクタリング

リファクタリングは原則として、機能追加から分離し、専用コミット・専用Pull Requestで行います。例外的に同時実施が必要な場合は、理由と影響範囲をPull Requestへ明記します。

## 設計原則

- 教材管理は、教材・セクション・問題のマスターデータを管理します。
- 演習は、解答履歴・部分点・確信度・周回・復習状態を管理します。
- ダッシュボードは、学習履歴から統計と可視化を生成します。
- 履歴を保持するために教材マスターを複製しません。
- 集計可能な値は原則として履歴から導出し、重複した状態を増やしません。

詳細は[アーキテクチャ](docs/architecture.md)を参照してください。
